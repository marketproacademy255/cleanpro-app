[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# React Router (client-side routing) - send every non-file, non-function
# request to index.html so deep links like /kabinet/buyurtma/:id work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
